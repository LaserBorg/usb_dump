[Unit]
Description=Mount USB Drive on %i using udevil and run usb_dump.sh
BindsTo=dev-%i.device
After=dev-%i.device

[Service]
Type=oneshot
RemainAfterExit=yes
User=pi
ExecStart=/bin/sh -c '/usr/bin/udevil mount /dev/%I > /tmp/usb_dump-%I.log 2>&1 && $USB_DUMP_DIR/usb_dump.sh /dev/%I'